using System;

namespace PPTail
{
    public static class Constants
    {
        public const String VALIDATEONLY_SWITCH = "--validateonly";
        public const String VERBOSE_SWITCH = "--verbose";
    }
}
